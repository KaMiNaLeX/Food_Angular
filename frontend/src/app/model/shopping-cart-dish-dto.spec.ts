import { ShoppingCartDishDto } from './shopping-cart-dish-dto';

describe('ShoppingCartDishDto', () => {
  it('should create an instance', () => {
    expect(new ShoppingCartDishDto()).toBeTruthy();
  });
});
